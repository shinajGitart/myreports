# Shinaj Work Report — Setup Commands
# Run these in order inside the Self_Report folder

# Step 1: Install all dependencies (React, Vite, Tailwind, PostCSS, etc.)
npm install react react-dom @vitejs/plugin-react vite autoprefixer postcss tailwindcss@3

# Step 2: Start the dev server
npm run dev
